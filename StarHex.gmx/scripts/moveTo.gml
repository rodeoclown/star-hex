///moveActor( actor, targetHex )
{
    var actor = argument0;
    var new_q = argument1;
 
    // If the actor isn't currently positioned on a hex   
    // Check the actor is moving to a neighbouring hex
}
